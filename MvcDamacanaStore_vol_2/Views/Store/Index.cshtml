@model IEnumerable<MvcDamacanaStore.Models.Bayii>
@{
    ViewBag.Title = "Store";
}
<h3>Browse Bayiler</h3>
<p>
    Select from @Model.Count()
    bayiler :
</p>
<ul>
    @foreach (var genre in Model)
    {
        <li>
            @Html.ActionLink(genre.Name,"Browse", new { genre = genre.Name })
    </li>
    }
</ul>
